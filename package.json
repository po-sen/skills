{
  "name": "skills",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run format:check"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
